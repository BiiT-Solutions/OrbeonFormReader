# Orbeon Form Reader

Import Orbeon forms into submitted form


# Usage

This is a Jar library that can be imported in any maven project as follows:

```
<dependency>
    <groupId>com.biit-solutions</groupId>
    <artifactId>orbeon-form-reader</artifactId>
</dependency>
```